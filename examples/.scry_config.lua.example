-- scry project config example
-- Place at: .scry_config.lua in your project root
-- Overrides global config values (deep merge)

return {
  general = {
    theme = "light",
    default_page_size = 50,
  },
  connections = {
    project_db = {
      type = "sqlite",
      database = "./data/app.db",
      read_only = true,
    },
  },
}
